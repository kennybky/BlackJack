import { CardValueDirective } from './card-value.directive';

describe('CardValueDirective', () => {
  it('should create an instance', () => {
    const directive = new CardValueDirective();
    expect(directive).toBeTruthy();
  });
});
